### Deaths incidence {.tabset}

#### By region
```{r reg-death, cache=TRUE, cache.extra=results_hash}
make.plots(noisy_deaths, by = "region", data = dth.dat, x.label = "Date of death")
```

#### By age
```{r age-death, cache=TRUE, cache.extra=results_hash}
make.plots(noisy_deaths, by = "age", data = dth.dat, x.label = "Date of death")
```

### Cumulative deaths {.tabset}

#### By region
```{r cum-reg-death, cache=TRUE, cache.extra=results_hash}
make.plots(cum_deaths, by = "region", x.label = "Date of death")
```

#### By age
```{r cum-age-death, cache=TRUE, cache.extra=results_hash}
make.plots(cum_deaths, by = "age", x.label = "Date of death")
```
